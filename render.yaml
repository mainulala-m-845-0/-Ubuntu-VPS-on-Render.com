services:
  - type: web
    name: ubuntu-vps
    env: docker
    plan: free
    healthCheckPath: /healthz
    envVars:
      - key: VPS_USER
        value: ubuntu
      - key: VPS_PASS
        value: ubuntu123
